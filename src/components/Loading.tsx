import React from 'react';

const LoadingSpinner: React.FC = () => {
  return (
    <div className="mt-5 pt-5 loading-spinner">
      <div className="spinner"></div>
    </div>
  );
};

export default LoadingSpinner;
